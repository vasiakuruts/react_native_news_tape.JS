import React from "react";
import { Text, View } from "react-native";
import { gStyle } from "../../components/g-styles/style";

const ContactPage = () => {
  return (
    <View style={gStyle.main}>
      <Text style={gStyle.title}>Контакти</Text>
    </View>
  );
};

export default ContactPage;
